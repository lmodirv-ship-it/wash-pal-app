
## حجم منصة H&Lavage الإجمالي (غير مضغوط)

### 📦 المجموع الكلي: **~395 MB**

| المكوّن | الحجم |
|---------|--------|
| `node_modules/` (مكتبات التطوير) | **~380 MB** |
| `src/` (الكود المصدري) | **~2 MB** |
| `public/` (الأصول العامة) | **~0.5 MB** |
| `src/assets/` (فيديو + صور) | **~11 MB** |
| `supabase/` (migrations + functions) | **~0.5 MB** |
| ملفات إعداد (package.json, configs...) | **~1 MB** |

---

### 🎯 الجواب المباشر

**حجم المنصة كاملة غير مضغوطة ≈ 395 MB** (أي تقريباً **400 ميجا**)

لكن إذا قصدت **بدون `node_modules`** (لأنها تُعاد بناؤها):
- **حجم المشروع الفعلي ≈ 15 MB فقط**
