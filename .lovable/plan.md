سأصلح هذا بشكل مباشر عند الموافقة:

1. أعدّل نظام الأزرار الأساسي في `src/components/ui/button.tsx` بحيث كل أزرار `Button` تكون باللون الأزرق المضيء.
2. أعدّل أنماط الأزرار في `src/index.css`، خصوصاً:
   - `.btn-pill`
   - `.btn-pill--primary`
   - `.btn-pill--secondary`
   - `.btn-pill--outline`
   - `.btn-pill--ghost`
   - `.btn-pill--destructive`
   - `.btn-glass`
3. أضيف قاعدة عامة تطبق على أي `<button>` عادي في كل الصفحات، حتى الأزرار التي لم تستخدم مكون `Button`.
4. أحافظ على الخلفية سوداء داكنة كما طلبت، وأجعل الأزرار فقط زرقاء مضيئة بتوهج واضح.
5. أزيل أو أتجنب أي تأثير يغير ألوان باقي العناصر مثل الجداول، البطاقات، النصوص، الروابط غير الزرية.

النتيجة المطلوبة:
- كل الأزرار في جميع الصفحات: أزرق مضيء.
- الخلفية: سوداء داكنة.
- باقي العناصر لا تتحول إلى أزرق إلا إذا كانت زرّاً.